# Pinned to what is installed and verified in this environment (Python 3.12).
# Phase 1 (eval harness) needs only: numpy, pandas, pillow, scikit-learn, tqdm, pyyaml.
# The rest are pinned now because §2 fixes the final dependency set; they are
# first imported in Phase 3+. Nothing here is unused by the final pipeline.

# --- Phase 1: evaluation harness ---
numpy==2.1.3
pandas==3.0.3
pillow==12.2.0
scikit-learn==1.8.0
tqdm==4.68.3
pyyaml==6.0.3

# --- Phase 3+: model, training, features ---
torch==2.13.0
torchvision==0.28.0
open-clip-torch==3.3.0
opencv-python==5.0.0.93

# --- reporting / demo ---
matplotlib==3.10.9
gradio==6.19.0

# --- dev ---
pytest==9.1.1
